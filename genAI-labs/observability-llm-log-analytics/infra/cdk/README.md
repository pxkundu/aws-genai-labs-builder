# CDK Notes

Terraform is the recommended provisioning tool for this POC. Use this folder to experiment with CDK stacks if desired (e.g., provisioning the OpenSearch domain or Lambda). Future work can include constructing a CDK app mirroring `infra/terraform` for teams standardised on CDK.
