apiVersion: v1
kind: Service
metadata:
  name: genai-minimal-starter-service
  namespace: genai-minimal-starter
  labels:
    app.kubernetes.io/name: genai-minimal-starter
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: genai-minimal-starter
    app.kubernetes.io/component: backend
